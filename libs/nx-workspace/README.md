# nx-workspace

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build nx-workspace` to build the library.

## Running unit tests

Run `nx test nx-workspace` to execute the unit tests via [Jest](https://jestjs.io).
